# https://github.com/bazelbuild/bazel/releases/tag/6.1.1
# bazel 6.2.1-homebrew

build --cxxopt=-g
build --cxxopt=-O3
build --cxxopt=-std=c++20
build --cxxopt=-Wall
build --cxxopt=-Werror
build --cxxopt=-fdiagnostics-color
build --cxxopt=-Wfatal-errors

# build --cxxopt=-Wno-restrict
# build --cxxopt=-Wno-non-template-friend
# build --cxxopt=-Wno-sign-compare

# build --cxxopt=-Wno-error=restrict
# build --cxxopt=-Wno-error=non-template-friend
# build --cxxopt=-Wno-error=sign-compare

# build --cxxopt=-fsanitize=address

build --spawn_strategy=standalone
