[cc_binary(
    name = cc_name.split('.')[0],
    srcs = [cc_name],
    copts = [
        "-std=c++2b",
        "-Wall",
        "-Wno-error",
    ],
) for cc_name in glob(
    include = ["**/*.cpp"],
)]
